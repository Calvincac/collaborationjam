export default class Beacon {

    constructor(id ='', local ='', nome ="", mensagem ="" ) {
        this.id = id;
        this.local = local;
        this.nome =  nome;
        this.mensagem = mensagem;
 
    }
}